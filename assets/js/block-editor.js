/**
 * Scripts pour l'éditeur de blocs WordPress
 * 
 * Ce fichier contient les scripts JavaScript pour améliorer
 * l'expérience dans l'éditeur Gutenberg.
 */

(function() {
    'use strict';

    // Scripts pour l'éditeur de blocs
    // Ajoutez ici vos scripts personnalisés pour l'éditeur
    
    // Exemple : Log pour vérifier que le script est chargé
    if (typeof console !== 'undefined') {
        console.log('G2RD Block Editor Scripts loaded');
    }
})();

